import type { Metadata } from "next";
import "./globals.css";
import Navbar from "@/components/Navbar";
import Footer from "@/components/Footer";

export const metadata: Metadata = {
  title: "Northbridge Systems | Practical Technology Solutions",
  description: "Practical technology solutions for growing UK businesses. IT support, cloud solutions, infrastructure management, and technical consultancy.",
  keywords: "IT support, cloud solutions, infrastructure, technical consultancy, business technology, UK",
  icons: {
    icon: '/favicon.svg',
  },
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" className="h-full">
      <body className="min-h-full flex flex-col">
        <Navbar />
        <main className="flex-grow">{children}</main>
        <Footer />
      </body>
    </html>
  );
}
